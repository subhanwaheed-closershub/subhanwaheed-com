# subhanwaheed.com - personal site.
#
# Nothing here is private. The anti-spam policy at https://subhanwaheed.com/#anti-spam is
# published deliberately so blocklist operators and abuse desks can find and read it, which is
# also why _headers carries no noindex. An openly permissive file here reinforces that.
#
# No Disallow lines, and that is deliberate rather than an oversight: there is nothing on this
# site that should not be crawled.

User-agent: *
Allow: /

# AI crawlers are explicitly welcome.
#
# These are stacked under ONE rule block on purpose. In robots.txt a crawler obeys only the most
# specific group that names it and ignores every other group, so giving each bot its own block
# with its own Allow would silently opt every one of them OUT of everything written above.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: anthropic-ai
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: CCBot
User-agent: meta-externalagent
User-agent: Amazonbot
User-agent: cohere-ai
Allow: /
